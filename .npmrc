# Expo + Metro, pnpm'in symlink node_modules yapisini sevmez.
# hoisted linker, paketleri duz bir node_modules'a serer -> Metro sorunsuz calisir.
node-linker=hoisted
shamefully-hoist=true

# Allow native module builds
build-scripts-list=better-sqlite3,msgpackr-extract
